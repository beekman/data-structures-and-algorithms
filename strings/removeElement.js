var removeElement = function(nums, val) {
  //set up a counter for time that val is in nums
  //itterate through nums to check for val
  //up the couter for each value that is not val
  //if there remove it from nums
  //decrement index if removing an element
  //return that counter
};
