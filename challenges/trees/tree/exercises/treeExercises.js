// Implement a function that sums all the values in a tree
const sumAllValuesInTree = (root) => {
  console.log('Summed!');
}

// Implements a function that averages all the values in a tree
const averageAllValueInTree = (root) => {
  console.log('Averaged!');
}

// Implement a function that searches for a specific value
// in a tree
const searchTreeForValue = (root, val) => {
  console.log('Found!');
}

module.exports = {
  sumAllValuesInTree,
  averageAllValueInTree,
  searchTreeForValue
}
